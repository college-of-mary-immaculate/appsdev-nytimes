{
  "name": "nytimes",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel serve src/index.html",
    "build": "parcel build src/index.html && cp src/mock.json dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
